/**
 * TODO: Replace `import Ember from 'ember'` with import of escapeExpression from appropriate module, when supported in Ember 3.x or 4.x:
 */
import Ember from 'ember';

export const { escapeExpression } = Ember.Handlebars.Utils; // eslint-disable-line import/prefer-default-export
